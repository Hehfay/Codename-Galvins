using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.EventSystems;

// Add this component to a Pickup item to have its name and count logged.
// In the future I would like to have a tool tip or something else.
public class HoverHandler : MonoBehaviour {
    /*
    void OnMouseEnter () {
        Pickup pickUpCopy = gameObject.GetComponent<Pickup> ();
        Debug.Log (pickUpCopy.pickupData.equipmentName + " x" + pickUpCopy.count);
    }
    */
}